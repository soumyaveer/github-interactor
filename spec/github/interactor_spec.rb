# frozen_string_literal: true

RSpec.describe Github::Interactor do
  it "has a version number" do
    expect(Github::Interactor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
